// GENERATED FROM crates/butler-core/src/ipc.rs (IPC v1.0); do not edit
// Regenerate: cargo run -p butler-desktop --bin export-bindings

// This file was generated by [tauri-specta](https://github.com/oscartbeaumont/tauri-specta). Do not edit this file manually.

/** user-defined commands **/


export const commands = {
/**
 * The contract version the UI checks at startup (§3.4).
 * 
 * Returned as a two-element tuple, which the generated TypeScript renders as
 * `[number, number]`. The UI compares only the major: a minor difference is
 * additive by the `constrains` edge on `ipc.rs` and therefore compatible.
 */
async getContractVersion() : Promise<[number, number]> {
    return await TAURI_INVOKE("get_contract_version");
},
/**
 * Make the overlay accept clicks, or stop (§3.3's Interact toggle).
 * 
 * # Errors
 * 
 * [`ErrorKind::Internal`] if the overlay window is gone, or if the platform
 * refuses the change. The stored flag is updated only after the platform
 * accepts it: recording a value the window is not actually in would leave
 * the next toggle flipping away from a state that never existed, and the
 * overlay stuck eating clicks with no way back.
 */
async setInteractive(on: boolean) : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("set_interactive", { on }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Start watching the screen.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Internal`] until spec 019 supplies the runtime.
 */
async arm() : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("arm") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Stop watching the screen.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Internal`] until spec 019 supplies the runtime.
 */
async disarm() : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("disarm") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Capture and ask now, bypassing change detection once.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Internal`] until spec 019 supplies the runtime.
 */
async askNow() : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("ask_now") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Clear the current answer.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Internal`] until spec 019 supplies the runtime.
 */
async dismiss() : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("dismiss") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Re-run the capture-exclusion self-test.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Internal`] until spec 005 supplies the self-test.
 */
async runSelfTest() : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("run_self_test") };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
},
/**
 * Put a provider credential in the OS keychain.
 * 
 * Spec 011 §3.2 and spec 015 §3.1: the credential is taken by value, is
 * never logged, printed or returned, and is dropped before this function
 * returns. `provider` is a configured label and is not a credential.
 * 
 * The keychain write and the zeroizing `Secret` that owns it are spec 010's.
 * Until then this refuses rather than accepting a credential it has nowhere
 * safe to put: accepting and discarding one would tell the user their key
 * was stored when it was not.
 * 
 * # Errors
 * 
 * Always [`ErrorKind::Credential`] until spec 010 supplies the secret store.
 */
async storeSecret(provider: string, secret: string) : Promise<Result<null, ErrorKind>> {
    try {
    return { status: "ok", data: await TAURI_INVOKE("store_secret", { provider, secret }) };
} catch (e) {
    if(e instanceof Error) throw e;
    else return { status: "error", error: e  as any };
}
}
}

/** user-defined events **/



/** user-defined constants **/



/** user-defined types **/

/**
 * The closed set of failure kinds the pipeline reports.
 * 
 * A kind, never a message: an error string built from screen content would
 * carry the user's data across the privacy boundary (spec 015). Spec 011's
 * IPC contract re-exports this enum as the wire `ErrorKind`, which is why it
 * carries the serde and specta derives the rest of this module does not: it
 * is the one reducer type that is also a wire type (spec 011 D-1).
 */
export type ErrorKind = 
/**
 * The display could not be captured.
 */
"capture" | 
/**
 * Text recognition failed.
 */
"ocr" | 
/**
 * The request never reached the provider.
 */
"network" | 
/**
 * The provider answered with an error.
 */
"provider" | 
/**
 * No usable credential for the configured provider.
 */
"credential" | 
/**
 * The spend guard refused the request.
 */
"budget" | 
/**
 * A defect on our side.
 */
"internal"
/**
 * The capture-exclusion status as the overlay sees it.
 * 
 * The wire mirror of [`ExclusionState`]. Constitution §VI is why this is
 * five values and not a boolean: the product reports what it verified, and
 * "asked for and not confirmed" is a different thing from "confirmed".
 */
export type ExclusionSummary = 
/**
 * Nothing reported yet; the self-test has not run.
 */
"unknown" | 
/**
 * The platform accepted the request; not yet confirmed.
 */
"applied" | 
/**
 * Applied and confirmed absent from a capture of the display.
 */
"verified" | 
/**
 * Applied, but the self-test saw the overlay's own pixels.
 */
"compromised" | 
/**
 * The platform cannot exclude this window.
 */
"unsupported"
/**
 * Which operating-system permission the app is waiting on.
 * 
 * One value, and deliberately so. Spec 004 §3.5 requests Screen Recording on
 * macOS and nothing else; Accessibility is never requested because the app
 * never synthesizes input, and Windows needs no grant for desktop
 * duplication. A second variant here would be a spec change, not a detail.
 */
export type PermissionKind = 
/**
 * macOS Screen Recording (TCC).
 */
"screen-recording"
/**
 * Which state the machine is in, without the payload it carries.
 * 
 * The UI draws a status strip, not a state machine. It needs the name and
 * nothing else; [`State`]'s sessions, cycles and boxed resume states are the
 * reducer's business and would be a second, lagging copy of the truth if
 * they crossed (spec 011 §1: the UI is a renderer, not a second brain).
 */
export type StateName = 
/**
 * Not watching the screen.
 */
"disarmed" | 
/**
 * Watching, with exclusion verified.
 */
"armed" | 
/**
 * Watching, with exclusion not verified and the user's consent.
 */
"degraded" | 
/**
 * A stage failed and a retry is counting down.
 */
"fault"
/**
 * Why the model stopped producing tokens.
 */
export type StopSummary = 
/**
 * The model finished its answer.
 */
"end-turn" | 
/**
 * The output token cap was reached.
 */
"max-tokens" | 
/**
 * The model declined to answer.
 */
"refusal" | 
/**
 * Anything else the provider reported.
 */
"other"
/**
 * Rust to UI. One channel, one tagged payload (§3.2).
 */
export type UiEvent = 
/**
 * The runtime's current state, emitted on every transition.
 */
{ type: "runtime-status"; state: StateName; seq: number; request: number | null; exclusion: ExclusionSummary; last_error: ErrorKind | null; armed_for_ticks: number } | 
/**
 * An inference began.
 */
{ type: "answer-started"; request: number } | 
/**
 * An inference finished.
 */
{ type: "answer-done"; request: number; stop: StopSummary } | 
/**
 * An inference failed.
 */
{ type: "answer-failed"; request: number; kind: ErrorKind } | 
/**
 * No usable credential for the configured provider.
 */
{ type: "needs-credential"; provider: string } | 
/**
 * An operating-system permission is missing.
 */
{ type: "needs-permission"; permission: PermissionKind } | 
/**
 * The capture-exclusion self-test reported (spec 005).
 */
{ type: "self-test-result"; verdict: ExclusionSummary }

/** tauri-specta globals **/

import {
	invoke as TAURI_INVOKE,
	Channel as TAURI_CHANNEL,
} from "@tauri-apps/api/core";
import * as TAURI_API_EVENT from "@tauri-apps/api/event";
import { type WebviewWindow as __WebviewWindow__ } from "@tauri-apps/api/webviewWindow";

type __EventObj__<T> = {
	listen: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.listen<T>>;
	once: (
		cb: TAURI_API_EVENT.EventCallback<T>,
	) => ReturnType<typeof TAURI_API_EVENT.once<T>>;
	emit: null extends T
		? (payload?: T) => ReturnType<typeof TAURI_API_EVENT.emit>
		: (payload: T) => ReturnType<typeof TAURI_API_EVENT.emit>;
};

export type Result<T, E> =
	| { status: "ok"; data: T }
	| { status: "error"; error: E };

function __makeEvents__<T extends Record<string, any>>(
	mappings: Record<keyof T, string>,
) {
	return new Proxy(
		{} as unknown as {
			[K in keyof T]: __EventObj__<T[K]> & {
				(handle: __WebviewWindow__): __EventObj__<T[K]>;
			};
		},
		{
			get: (_, event) => {
				const name = mappings[event as keyof T];

				return new Proxy((() => {}) as any, {
					apply: (_, __, [window]: [__WebviewWindow__]) => ({
						listen: (arg: any) => window.listen(name, arg),
						once: (arg: any) => window.once(name, arg),
						emit: (arg: any) => window.emit(name, arg),
					}),
					get: (_, command: keyof __EventObj__<any>) => {
						switch (command) {
							case "listen":
								return (arg: any) => TAURI_API_EVENT.listen(name, arg);
							case "once":
								return (arg: any) => TAURI_API_EVENT.once(name, arg);
							case "emit":
								return (arg: any) => TAURI_API_EVENT.emit(name, arg);
						}
					},
				});
			},
		},
	);
}

/** contract constants (spec 011 §3.2, §3.4) **/

export const EVENT_CHANNEL = "butler://event" as const;
export const IPC_CONTRACT_VERSION = [1, 0] as const;
